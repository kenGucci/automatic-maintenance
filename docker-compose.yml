version: "3.9"

services:
  automend:
    build: .
    ports:
      - "8080:8080"
      - "9090:9090"
    env_file: .env
    restart: unless-stopped
    environment:
      - AGENT_URL=http://localhost:9090
